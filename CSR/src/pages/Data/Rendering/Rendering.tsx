export const Rendering = () => {
  return (
    <div>
      <h1>Rendering</h1>
      <p>
        Rendering is the process of generating a visual representation of the
        DOM (Document Object Model) for the user. The browser parses the HTML
        and CSS files to create the DOM and CSSOM (CSS Object Model) and then
        combines them to render the page on the screen.
      </p>
    </div>
  );
};
